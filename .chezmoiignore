# Source-only files. Anything in the source root that does not begin with a dot
# otherwise becomes a target and gets written to the home directory.
#
# winget-packages.json is read by run_onchange_01 via `include`; .chezmoiignore
# only affects what is applied, not what templates can read.
winget-packages.json
README.md
